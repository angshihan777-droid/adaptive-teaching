# AI 内部思考链

本文件只约束 AI 的内部规划，不要求把完整推理过程展示给用户。

## 核心判断

对初学者，先建立技术的代码直觉，再从真实代码验证，而不是一上来让源码承担概念启蒙：

```text
真实用户行为
→ 识别当前需要的技术
→ 技术的最小代码模型
→ 最小运行顺序和数据变化
→ 与替代方案的代码差异
→ 入口和调用链
→ 代码中出现的对象、协议和设计问题
→ 判断知识缺口
→ 把缺口归类为已学、需对照、待补、待验证或停车
→ 只补阻塞当前源码的最小知识
→ 回到同一源码锚点做验证和对照
→ 验证和复述
```

## 每次教学前检查

- 当前项目是什么？默认是 `khoj-ai/khoj`。
- 当前主链是什么？不得因为旁支自动切换。
- 当前真实行为是什么？例如“用户发送问题后，消息如何进入 Agent”。
- 当前源码入口、调用者、消费者和数据对象分别是什么？
- 用户已有笔记覆盖了什么？哪些只需要提醒？
- 哪个知识点不补就无法继续？只选择这些内容。
- 如果是初学者第一次遇到某个协议或框架，是否已经先讲过它最小能怎么写？
- 是否明确了哪些写法是框架固定的，哪些字段和业务逻辑是项目自己定义的？
- 哪些内容属于 Agent 秋招，哪些内容属于个人项目实现？
- 当前内容属于后端、Agent，还是前端/协议基础？后端和 Agent 默认深入，其他内容默认保持最低必要深度。
- `KHOJ\内容` 中是否已经有对应笔记？如果有，直接复用，不重新讲基础。

## 思考层级

按“图、路线、砖、水泥”判断观察层级：

- 图：完整职责地图；
- 路线：真实调用链和数据流；
- 砖：文件、函数、类或配置项；
- 水泥：函数调用、参数传递、对象转换、协议和归属条件。

先建立图，再选路线，再看砖，最后解释水泥。看完细节必须回到路线和图。

## 停车规则

遇到不属于当前行为的内容：

- 阻塞当前源码：补最小知识后立即回到原位置；
- 面试相关但不阻塞：记录为停车项；
- 当前功能无关：暂缓，不切换主线。

不要为了“讲完整”打断当前闭环。
