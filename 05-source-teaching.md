# 源码线：按真实行为跨文件追踪

源码课从用户行为或系统行为出发，不按目录顺序通读。它的职责是围绕概念课留下的知识锚点，验证真实项目如何实现设计，而不是第一次介绍陌生技术或重新讲设计理论。

## 不同模块的源码深度

- 后端和 Agent：跨文件追踪真实调用链，解释输入、转换、输出、失败和设计原因。
- 前端：只追踪与后端契约、流式事件和用户动作直接相关的代码；页面布局和组件细节停车。
- 通信协议：先用最小示例建立代码直觉，再只阅读项目中与当前行为有关的实现。
- 框架内部：默认只说明职责和输入输出，不进入框架源码，除非当前故障无法定位。

## 进入源码前的前置检查

如果当前协议、框架或架构对学习者仍然陌生，先退回概念课，确认已经理解：

- 最小代码结构；
- 运行时先后顺序；
- 输入、输出和结束条件；
- 框架固定部分与项目自定义部分。

没有这些接点时，不直接展示一大段真实源码并要求学习者从中推导技术原理。

## 源码课的固定开场

源码课必须先承接概念课，而不是直接展示代码。固定顺序是：

1. 回顾概念课的知识锚点和工程问题；
2. 说明当前源码要验证锚点中的哪条设计；
3. 说明本段源码会出现哪些输入、转换和输出；
4. 再展示一小段连续源码；
5. 解释源码如何实现概念课中的设计；
6. 说明这段实现对接口文档、交接和故障定位意味着什么。

禁止使用“标题 + 一句结论 + 代码”的开场方式。这样会让初学者被迫从源码反推概念。

## 按概念锚点带读源码

源码带读必须与概念课的顺序保持一致。每个概念锚点使用以下节奏：

1. 重述当前概念解决的问题；
2. 指出对应文件和代码位置；
3. 展示只覆盖该概念的连续代码；
4. 解释输入、转换、输出和设计原因；
5. 说明结果如何交给下一个概念。

例如概念是“从请求对象取出问题和会话 ID”，就先展示 `q = body.q` 和 `conversation_id = body.conversation_id`，不要同时把用户、会话、Agent 和历史代码全部贴出来。

## 追踪顺序

```text
触发者
→ 入口文件和函数
→ 输入数据
→ 参数或对象转换
→ 下一个函数或服务
→ 数据库、检索、工具或模型
→ 返回值或事件
→ 最终消费者
→ 正常结束与失败边界
```

## 每段源码必须说清楚

- 代码解决什么业务问题；
- 调用者是谁；
- 输入从哪里来；
- 数据变成了什么；
- 为什么职责放在这一层；
- 输出交给谁；
- 出错时从哪里观察；
- 哪些细节本节暂不展开。

## 源码预览和解析分开

预览只定位文件、函数、前后调用者、输入输出和本次观察动作，不解析陌生语法。解析时再解释连续业务代码，并在结束时写出：

```markdown
## 这段源码接上了什么
```

说明它接回主链的哪一段、下一步从哪里继续。

## 代码来源模板

第一次出现一个函数、类、参数或属性时，说明：

- 定义文件；
- 谁导入或调用；
- 调用时机；
- 输入来源；
- 输出去向；
- 当前为什么需要它。

不要把“这个函数负责……”当成完整的调用链解释。
