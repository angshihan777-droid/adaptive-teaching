# 工程思维与技术选型分析

遇到协议、框架、模块或架构设计时，不只解释“是什么”，还要解释“为什么在这里需要它”。

## 固定分析框架

```text
技术或设计对象
→ 所属分类
→ 解决的真实问题
→ 不使用它会怎样
→ 常见替代方案
→ 与替代方案的关键差异
→ 当前项目选择它的源码和行为证据
→ 选择带来的成本和失败边界
→ 我的最小 Web 项目保留、简化还是替换
```

## 必须区分三种结论

- `源码事实`：能在文件、函数、配置、前端消费或运行结果中找到证据。
- `工程推断`：根据行为和常见设计做出的合理解释，必须标注为推断。
- `待验证`：目前没有足够证据，列出下一步要检查的代码或实验。

## 以通信方式为例

分析 WebSocket 时至少比较：

- HTTP：一次请求对应一次响应；
- SSE：服务端向客户端单向持续推送；
- WebSocket：客户端和服务端都能持续发送消息；
- 长轮询：客户端反复等待和重新请求。

然后结合当前行为判断：是否需要中断、双向事件、持续连接、连接级状态或复杂实时交互。不能因为“聊天常用 WebSocket”就直接断言这是 Khoj 的选型原因。

## 产品视角

每次分析都补充最小产品问题：

- 用户真正需要什么反馈？
- 哪些能力是 MVP 必须的？
- 哪些复杂性只是原项目为更大规模准备的？
- 如果流量、用户、工具数量增加，哪里会先出问题？

个人项目默认采用“能证明核心能力的最小方案”，而不是复制原项目全部复杂度。
