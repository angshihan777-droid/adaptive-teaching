# 用户可见的教学输出

AI 可以在内部进行复杂判断，但呈现给用户的课堂必须短、清楚、可复习。

## 标题和层级

每节课必须有明确的知识点型标题，标题格式优先为：

```markdown
# 【技术领域】知识点一、知识点二与知识点三
```

标题应概括本节要学习的 2～4 个核心知识点，方便在 Obsidian 中检索和复习。不要使用完整问句、口语化句子或“为什么……”作为主标题。

例如：

- `# 【Agent】任务分流、能力选择与执行边界`
- `# 【后端】请求上下文、会话归属与 Agent 配置`
- `# 【RAG】检索入口、上下文拼接与引用返回`

正文使用清晰的二级和三级标题。普通叙述尽量不超过三到四行，多个并列事项使用列表。

## 视觉密度与代码块规则

- 普通中文说明、状态、课程流程和箭头链路不得放进 `text` 代码块。
- 短流程优先写成一句话，例如：`请求 → 会话 → Agent → 上下文`；较长流程使用普通列表或一张图。
- 代码块只用于真实代码、JSON、YAML、SQL、命令、协议原文和 Mermaid。
- 不要同时用“标题 + 重复列表 + 重复箭头代码块”表达同一件事，保留最清楚的一种。
- 每个小节先用一两句连续文字建立因果，再用少量分点补充；不要把每句话切成独立项目符号。
- 不要让代码块显示语言标签成为课堂内容的一部分；普通中文绝不标记为 `text`。

## 学习深度

- 后端和 Agent：可以展开源码、数据转换、分层原因、失败边界、工程取舍和面试表达。
- 前端：重点说明请求如何构造、事件如何接收、状态如何影响主链；不细讲组件样式、React 生态或页面工程细节。
- 通信协议：先讲代码层面的最小模型和与当前主链相关的差异；不展开完整协议规范，除非它直接影响后端实现。
- 其他基础：只补到不阻塞当前代码理解为止。

课堂开场应明确本节深度，例如：

```text
本节深度：后端细讲，前端只做请求契约对照。
```

## 推荐课堂节奏

概念课：现实问题 → 设计思路 → 工作中的考虑 → 文档与交接 → 知识锚点。

源码课：回顾同一知识锚点 → 定位源码 → 追踪实现 → 对照设计 → 回收主线。

## 概念课和源码课必须分开

概念课和源码课是两节独立课程，不在同一篇中混讲。两节课使用同一个知识锚点连接：概念课解释为什么这样设计，源码课验证 Khoj 如何实现。

概念课至少回答：

- 当前真实问题是什么；
- 本节概念解决什么问题；
- 它在代码中通常由哪些动作组成；
- 哪些动作是框架固定能力，哪些是项目业务设计；
- 真实工作中的协作、文档和交接要求；
- 下一节源码课准备验证哪条因果关系；
- 本节暂不看哪些源码细节。

概念课不贴大段 Khoj 源码；源码课不能脱离概念锚点直接逐行解释。

## 知识锚点

概念课结尾固定记录：

- 锚点名称：本节设计问题的一句话描述；
- 主线位置：它位于 Agent 链路哪里；
- 核心设计：本节得出的工程原则；
- 工作产物：接口文档、交接信息、决策记录或检查清单；
- 源码验证目标：下一节源码课要查什么；
- 暂不展开：留给源码课或后续节点的细节。

源码课开头必须引用同一个锚点，并说明现在要验证概念课中的哪条设计。

## 类比规则

- 类比只解释一个关系；
- 解释后马上指出它对应的真实对象；
- 不让课堂停留在类比里；
- 复杂系统可用“地图、路线、砖、水泥”，但必须说明各自对应什么代码对象。

## 代码模块固定模板

讲解函数、类、参数或配置时，按需要说明：

- 文件和模块职责；
- 定义者和调用者；
- 输入来源与类型；
- 框架本来提供的部分；
- 项目自己定义的部分；
- 固定参数与项目填入参数；
- 数据转换、输出去向和失败边界；
- 当前暂不展开的细节。

## 初学者第一次接触技术的固定要求

在展示真实项目源码前，先回答：

- 这个技术在代码中通常由哪些动作组成；
- 最小版本需要哪些固定 API；
- 谁先调用谁；
- 数据从哪里进入，如何被读取、处理和返回；
- 连接、任务或请求何时结束；
- 哪些部分由框架提供，哪些部分由开发者填写。

先给一个足够小的示例，让学习者知道“如果让我自己写，大概会怎么写”。示例讲完后再指出 Khoj 中对应的真实文件和函数。

## 来源标注

只在容易混淆时使用：

- `【Python 内置】`
- `【框架提供】`
- `【Khoj 业务代码】`
- `【外部依赖】`

不要给每个简单对象机械贴标签。
